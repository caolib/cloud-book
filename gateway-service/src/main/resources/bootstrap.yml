spring:
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: bin:8848
      config:
        file-extension: yml # 文件后缀名
      discovery:
        ephemeral: true
  application:
    name: ms-gateway