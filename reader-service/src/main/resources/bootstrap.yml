spring:
  application:
    name: ms-reader
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: bin:8848
      config:
        file-extension: yml # 文件后缀名
      discovery:
        ephemeral: true
#      discovery:
#        cluster-name: zh
#        weight: 1 # 权重
config:
  name: "cao"