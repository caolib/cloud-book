spring:
  profiles:
    active: dev
  cloud:
    nacos:
      #      server-addr: localhost:8848
      server-addr: bin:8848
      discovery:
        ephemeral: true
        heart-beat-timeout: 10000
    loadbalancer:
      cache:
        enabled: false
  application:
    name: ms-borrow