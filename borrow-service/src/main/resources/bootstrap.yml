spring:
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: localhost:8848
    #      discovery:
    #        heart-beat-timeout: 10000
    #        weight: 2
    loadbalancer:
      ribbon:
        enabled: false
  application:
    name: ms-borrow